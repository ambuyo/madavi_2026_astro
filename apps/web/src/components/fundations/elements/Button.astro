---
const {
  variant,
  size,
  onlyIconSize,
  iconOnly = false,
  gap,
  isLink = false,
  class: className,
  ...rest
} = Astro.props;
const defaultClass = [
  "text-white",
  "bg-black",
  "hover:bg-base-900",
  "focus:outline-base-300",
];
const accentClass = [
  "text-white",
  "bg-accent-400",
  "hover:bg-accent-500",
  "focus:outline-accent-600",
];
const mutedClass = [
  "text-base-950",
  "bg-base-50",
  "hover:bg-base-100",
  "focus:outline-base-300",
];

const mutedDarkClass = [
  "text-white",
  "bg-base-900",
  "hover:bg-base-800",
  "focus:outline-base-700",
];

// Size
const sizes = {
  xxs: ["h-7.5", "px-4", "py-2", "text-xs", "rounded-sm"],
  xs: ["h-8", "px-4", "py-3", "text-xs", "rounded-md"],
  sm: ["h-9", "px-4", "py-3", "text-sm", "rounded-md"],
  base: ["h-10", "px-5", "py-4", "text-base", "rounded-md"],
  md: ["h-11", "px-5", "py-4", "text-md", "rounded-md"],
  lg: ["h-12", "px-6", "py-4", "text-lg", "rounded-lg"],
  xl: ["h-13", "px-6", "py-4", "text-lg", "rounded-lg"],
};
// Icon size
const iconSizes = {
  xxs: ["size-7.5", "py-2", "text-xs", "rounded-sm"],
  xs: ["size-8", "text-xs", "rounded-md"],
  sm: ["size-9", "text-sm", "rounded-md"],
  base: ["size-10", "text-base", "rounded-md"],
  md: ["size-11", "text-md", "rounded-md"],
  lg: ["size-12", "text-lg", "rounded-lg"],
  xl: ["size-13", "text-base", "rounded-lg"],
};
// Gaps
const gapMap = {
  xs: ["gap-2"],
  sm: ["gap-4"],
  base: ["gap-8"],
  md: ["gap-10"],
  lg: ["gap-12"],
};
const variantClass = {
  default: defaultClass,
  accent: accentClass,
  muted: mutedClass,
  mutedDark: mutedDarkClass,
  none: [],
};
const sizeClass = iconOnly ? iconSizes[size] : sizes[size] || [];
const gapClass = !iconOnly && gap ? gapMap[gap] : [];
const additionalClasses = className ? className.split(" ") : [];
const baseClass = [
  "flex",
  "justify-center",
  "text-center",
  "font-medium",
  "items-center",
  "duration-500",
  "ease-in-out",
  "transition-colors",
  "focus:outline-2",
];
---

{
  isLink ? (
    <a
      href={rest.href}
      class:list={[
        ...baseClass,
        ...(variantClass[variant] || []),
        ...sizeClass,
        ...gapClass,
        ...additionalClasses,
      ]}
      {...rest}
    >
      {iconOnly ? (
        <slot name="icon" />
      ) : (
        <>
          <slot name="left-icon" />
          <slot />
          <slot name="right-icon" />
        </>
      )}
    </a>
  ) : (
    <button
      class:list={[
        ...baseClass,
        ...(variantClass[variant] || []),
        ...sizeClass,
        ...gapClass,
        ...additionalClasses,
      ]}
      {...rest}
    >
      {iconOnly ? (
        <slot name="icon" />
      ) : (
        <>
          <slot name="left-icon" />
          <slot />
          <slot name="right-icon" />
        </>
      )}
    </button>
  )
}
